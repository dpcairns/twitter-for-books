import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gutenscroll.app',
  appName: 'Gutenscroll - Doomscroll the classics',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
